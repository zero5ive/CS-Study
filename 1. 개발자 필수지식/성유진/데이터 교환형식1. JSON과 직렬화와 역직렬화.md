# JSON
JSON(JavaScript Object Notation)은 Javascript 객체 문법으로 구조화된 데이터교환 형식, python, javascript, java 등 여러 언어에서 데이터 교환형식으로 쓰이며 객체문법 말고도 단순배열, 문자열도 표현 가능



# 1. Javacript 객체문법
키(key)과 값(value)으로 구성됨. {key : value}

이미 존재하는 키를 중복선언하면 나중에 선언한 해당 키에 대응한 값이 덮어쓰이게 됨
<img width="1160" height="276" alt="image" src="https://github.com/user-attachments/assets/27dca076-edca-4a19-8ae8-4d6f871e3c6a" />




여러개를 표현하고 싶을 땐 배열로!

<img width="1118" height="356" alt="image" src="https://github.com/user-attachments/assets/8466e10a-8f50-4de5-92ab-649c4c1c7472" />





JSON을 javascript에서 사용할 때 자바스크립트 오브젝트(JSON.parse)로 변환해서 써야 함
<img width="1302" height="272" alt="image" src="https://github.com/user-attachments/assets/5a607c84-cfe7-494f-ab3c-384086e2d26b" />


path.join(__dirname,"a.json") 을 통해 JSON을 읽고, 

console.log(JSON.parse(a)) 이렇게 자바스크립트 오브젝트를 만들어서 

console.log(a[0]) 이렇게 출력!!

0번째 요소를 끄집어내고 싶으면 console.log(a[0])

0번째 요소의 name을 끄집어내고 싶으면 console.log(a[0].name)(console.log(a[0]["name"]) 이것도 됨)



json은 재귀적으로도 표현할 수 있음
<img width="1116" height="456" alt="image" src="https://github.com/user-attachments/assets/547c9292-f755-41bb-80b0-a3f86f1404d9" />




console.log(a[0]["name"].firstname) 이렇게 끄집어낼 수 있음 ( console.log(a[0].name.firstname)이렇게도 됨)





# 2. 데이터 + 교환형식 (대표적인 데이터 교환형식이 JSON이다.)
데이터는 추상적인 아이디어에서부터 시작해 구체적인 측정에 이르기까지 다양한 의미로 쓰인다. 실험, 조사, 관찰 등으로 부터 얻은 사실이나 자료 등을 의미한다.





# 3. 여러 언어에서의 쓰임
객체, 해시테이블, 딕셔너리 등으로 변환되어 쓰임

ex)  json in javascript = javascript object

       json in python = dict



JSON은 여러가지 언어 그리고 플랫폼에 대해서 독릭접임


자바스크립트인 경우
javasript object는 JSON.parse임, 어떤 로직을 구축하고 싶으면  자바스크립트에서는 이렇게 자바스크립트 오브젝트(JSON.parse)로 만들어 줘야 함

cnt라는 변수 선언 후 출력 (위에 JSON 선어한 거에 이어서)
<img width="1136" height="410" alt="image" src="https://github.com/user-attachments/assets/b7200357-cf0e-4ffd-989e-d6d86ef0cfcb" />


<img width="172" height="52" alt="image" src="https://github.com/user-attachments/assets/1f080256-c59e-4631-ac03-21b9ca48ee99" />









파이썬인 경우
json.loads(x)를 통해서 Python dictionary로 만들어 줌


<img width="1060" height="414" alt="image" src="https://github.com/user-attachments/assets/7078c253-69e8-45c6-9848-2e741b25d9a9" />






# 4. 단순배열, 문자열 표현
[1,2,3,4]

"파카파카파카파카"

위의 두개도 JSON임, 그치만 보통 이런형식으로 쓰이진 않음





JSON 실습 1

<img width="956" height="414" alt="image" src="https://github.com/user-attachments/assets/424d9cf6-91e5-404d-8bbd-28e63a6b3586" />

<img width="800" height="194" alt="image" src="https://github.com/user-attachments/assets/2d62c494-0ae5-4d18-a9dc-f8cb150b8c2b" />





JSON 실습 2

이름 : 큰돌

좋아하는 것 : 아령, 바나나

아령 : 10kg, 육각형

바나나 : 초록색

<img width="1074" height="436" alt="image" src="https://github.com/user-attachments/assets/d97a8704-c188-42e6-83b4-e4328b1b047b" />






# JSON의 타입
jacascript object와 유사하지만 undefinde, 메서드 등을 포함하지 않는다.

- 수 (Number)

- 문자열 (String) :

- 참/거짓 (Boolean)

- 배열 (Array)

- 객체 (object)

- null





# JSON 직렬화, 역직렬화
직렬화란 외부의 시스템에서도 사용할 수 있도록 바이트(byte) 형태로 데이터를 변환하는 기술이며 역직렬화는 반대를 의미

JSON.parse() (역직렬화) JSON 문자열 → JS 객체 (서버 응답을 자바스크립트 에서 사용하기 위해)

JSON.stringify() (직렬화) JS 객체 → JSON 문자열 (서버로 보내기 위해)


<img width="1602" height="778" alt="image" src="https://github.com/user-attachments/assets/38b04118-df49-453d-a11f-81b0b0f5ad33" />


<img width="1194" height="332" alt="image" src="https://github.com/user-attachments/assets/f9f83b4e-7863-4dad-ba66-646fed7024b5" />







# JSON의 활용
JSON은 프로그래밍 언어와 프레임워크 등에 독립적이므로, 서로 다른 시스템간에 데이터를 교환하기가 좋다. 주로 API의 반환형태, 시스템을 구성하는 설정파일에 활용된다.

ex) 업비트의 API

ex) package json
